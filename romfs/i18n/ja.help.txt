アルバムの Applet モードで実行すると、
一部の機能はメモリ制限を受けます。
一部の NSZ/XCZ は展開用メモリが不足します。

アプリケーションモードで Xxplore を実行すると、
より大きな画像を表示でき、
より大きなインストールキャッシュも使えます。

ドライブ名のルール:
sdmc: は SDカード を表します
ums*: は USBストレージ機器 を表します
名前(WebDAV): は WebDAV ドライブを表します
名前(SMB): は Samba/SMB ドライブを表します
名前(FTP): は FTP ドライブを表します
