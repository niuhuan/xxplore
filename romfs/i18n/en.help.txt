Drive naming rules:
sdmc: means the SD card
ums*: means a USB storage device
Name(WebDAV): means a WebDAV drive
Name(SMB): means a Samba/SMB drive

When Xxplore runs in application mode,
you can view larger images
and use a larger install cache.

If you run it in Album Applet mode,
these capabilities are limited by memory.
